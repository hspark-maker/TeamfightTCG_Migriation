// 스텝 실행이 실패했을 때 "어떻게 끝나는가" — 결말을 코드가 아니라 저작이 정한다
// 시퀀스에 int로 직렬화 → 새 값은 끝에만 추가(재배치·삭제 시 저작된 행이 엉뚱한 정책이 된다)
public enum EOutgameTutorialFailure
{
    Skip = 0,   // 실패해도 다음 칸으로 넘어간다(기본)
    Halt = 1,   // 좌표를 되돌려 그 자리에 세운다 — 안내는 멈추지만 남은 기능이 전부 열린다
}
