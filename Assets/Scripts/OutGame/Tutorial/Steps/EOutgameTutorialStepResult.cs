// 스텝 진입이 "무엇을 하고 끝났는가" — 호출자가 좌표 델타로 되짚지 않게 실행기가 직접 답한다
// (직렬화하지 않는 런타임 전용 값)
public enum EOutgameTutorialStepResult
{
    Gated,      // 스텝이 이 씬에 서 있다 — 앵커에 게이트를 걸고 완료 신호를 기다려야 한다
    Advanced,   // 좌표가 이미 다음 칸으로 넘어갔다(자동 스텝의 정상 통과 또는 Skip 정책의 실패)
    Failed,     // 좌표가 그대로다 — 이 씬에서 다시 세울 수단이 없으므로 기능 잠금을 연다
}
