# Cloud Run 전투 재생 서비스(비공개). 비어 있으면 battleReplayCloud 가 조용히 건너뛴다 —
# 서비스가 없거나 롤백해야 할 때 이 값을 지우면 섀도 호출만 꺼지고 정산은 그대로 돈다.
# 비밀값이 아니다(호출에는 IAM ID 토큰이 따로 필요하다).
BATTLE_REPLAY_URL=https://battle-replay-zy5ccormuq-du.a.run.app
