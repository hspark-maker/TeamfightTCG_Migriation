# TeamfightTCG_Migriation

## 언어

항상 한국어로 대답할 것.

## 스펙 데이터 수정 규칙

- 스펙 값의 저장소 진실원은 `docs/SpecData/*_sheet.csv`다. 값 변경은 CSV에만 한다.
- **스펙 데이터 작업 전 반드시 메모리 `C:/Users/cookapps/.claude/projects/C--Users-cookapps-TeamfightTCG-Migriation/memory/specdata-edit-csv-only.md`를 읽는다.** `SpecData.bytes`를 쓸 가능성이 있는 도구·명령 실행 전에도 재확인한다.
- **`Assets/Resources/SpecData.bytes`는 산출물이다. 에이전트가 수정·생성·덮어쓰기·재생성하지 않는다.** 직접 바이너리 편집뿐 아니라 `SpecLocalCsvImporter` 실행으로 갱신하는 것도 금지한다. 읽기·복호화 확인만 허용한다.
- CSV 변경은 CSV에서 끝낸다. CSV와 bytes가 다르다는 이유로 임포터를 자동 실행하지 않는다. 시트·생성 산출물 반영 여부는 별도로 보고한다.
- 정식 시트 스키마 변경이나 생성 산출물 갱신은 사용자의 별도 명시적 지시가 있을 때만 수행한다. 일반적인 스펙 값 변경 요청은 bytes 갱신 권한이 아니다.
- 자동 생성 파일 `Assets/Table/SpecDatas.cs`와 `Assets/Table/SpecDataManager.cs`를 수동 수정하지 않는다.

## Agent 활용 정책 (에이전트 자율 판단)

- 직접 처리할지 서브에이전트에 위임할지, 누구에게 무엇을 맡길지는 에이전트가 작업 성격과 현재 실행 환경을 보고 판단한다. 필요한 위임은 사용자에게 매번 확인하지 않고 진행한다.
- 독립 작업을 병렬로 처리하거나 별도 검토가 도움이 되면 위임한다. 직접 처리하는 편이 효율적이면 직접 진행한다. 특정 에이전트·팀 소집, 단계별 워크플로, 턴 수·토큰 수에 따른 위임을 강제하지 않는다.
- 커스텀 에이전트는 필요할 때 역할 설명을 확인해 선택한다. 검색·구현·검수 방법과 도구 선택도 작업에 맞게 판단한다.
- 위임 시 작업 범위와 필요한 결과를 명확히 전달하고, 반환된 근거를 확인한 뒤 최종 결과에 반영한다. 변경에 필요한 검증은 직접 수행하거나 적절히 위임한다.
- 위임 도구를 사용할 수 없는 환경에서는 직접 진행한다. 실제 호출 없이 위임했다고 보고하지 않는다.
- 사용자가 직접 처리 또는 특정 위임 방식을 지정하면 그 지시를 우선한다. 과거 메모리의 적극 위임·필수 소집 지침보다 이 정책을 우선한다.

## 기능 지도 — 상주 컨텍스트

자체 코드가 `Assets/Scripts/` 409파일 65,801줄이라 grep 부터 시작하면 헤맨다.
@.claude/orch-feature-map.md

지도의 타입 이름은 후보다 — `rg` 로 존재를 확인하고 진행한다.

측정치(같은 질문 3개, 지도 있음/없음): 탐색 총 입력 토큰 -67%, 턴 -59%, 도구 호출 -67%.

지도를 고쳤으면 `node .claude/check-feature-map.js` 로 타입 실재를 검증한다.

전체 파일 목록만 필요하면 Glob 또는 rg --files 로 현재 상태를 조회한다.
